game: snake
algorithm: alphazero

environment:
  width: 20
  height: 20

agent:
  learning_rate: 0.001
  weight_decay: 0.0001
  hidden_dim: 256
  num_simulations: 50
  c_puct: 1.5
  dirichlet_alpha: 0.3
  buffer_size: 10000

games_per_iteration: 25
train_steps_per_iteration: 500
